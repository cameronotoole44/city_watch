{
  "name": "city_watch",
  "version": "0.2.0",
  "description": "nopixel rp stream aggregator",
  "scripts": {
    "dev": "bun run server.ts",
    "start": "bun run server.ts",
    "sync": "bun run scripts/sync-streamers.ts"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.3.0"
  }
}